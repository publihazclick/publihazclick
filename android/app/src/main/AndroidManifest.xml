<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Reemplaza al MessagingService por defecto de @capacitor/push-notifications para poder
             armar la notificacion de pantalla completa de solicitudes de viaje. Ver
             MoviFirebaseMessagingService.kt. Sin este tools:node="remove", Android tendria 2
             servicios registrados para el mismo evento MESSAGING_EVENT (comportamiento no
             confiable, solo uno terminaria recibiendo los mensajes). -->
        <service
            android:name="com.capacitorjs.plugins.pushnotifications.MessagingService"
            tools:node="remove" />

        <service
            android:name=".MoviFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <receiver
            android:name=".MoviDriverWidget"
            android:exported="true"
            android:label="Movi Conductor">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_movi_driver_info" />
        </receiver>
    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- Causa real confirmada del "permission denied" al iniciar una llamada LiveKit
         (2026-08-02): BridgeWebChromeClient.onPermissionRequest (codigo propio de Capacitor,
         ver node_modules/@capacitor/android) SI intercepta el getUserMedia({audio:true}) del
         WebView y dispara el pedido de permiso nativo en runtime, pero solo puede pedir
         permisos que la app tenga DECLARADOS aca. Sin esta linea, Android niega el permiso en
         silencio antes de mostrar cualquier cuadro, y el JS recibe exactamente
         "NotAllowedError: Permission denied". Aplica igual a conductor y pasajero, mismo APK. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Requerida desde Android 14 (API 34) para que setFullScreenIntent() funcione. Sin esto,
         la notificacion de solicitud de viaje se degrada a una notificacion normal en la bandeja
         en vez de aparecer en pantalla completa con la app cerrada/celular bloqueado. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Causa real mas probable de "a veces llega la notificacion, a veces no" (2026-08-03):
         con la app cerrada/en segundo plano por un rato, Doze/App Standby de Android (agravado
         por los administradores de bateria propios de MIUI/Xiaomi, Samsung, Huawei, etc.)
         retrasa o descarta por completo los mensajes FCM en background de forma NO determinista,
         coincide exacto con el sintoma reportado (intermitente, no un fallo constante). Ver
         MoviPermissionsPlugin.kt (requestIgnoreBatteryOptimizations) y _grantDriverPermissions en
         anda-gana.component.ts, que ahora piden al conductor excluir la app de la optimizacion de
         bateria al ponerse en linea por primera vez. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
</manifest>
